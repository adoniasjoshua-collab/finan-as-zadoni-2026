# Deploy estatico

Esta versao esta preparada para deploy estatico sem build e sem backend.

O alvo recomendado agora e o GitHub Pages. Consulte `DEPLOY-GITHUB.md`.

Se publicar no Netlify, use:

- Build command: vazio
- Publish directory: `.`

Os dados continuam sendo gravados apenas no `localStorage` do navegador.
